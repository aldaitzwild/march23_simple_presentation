{% extends 'layout.html.twig' %}

{% block content %}
    <div class="row">
        <img src="{{product.photo_url}}" class="col-2">
        <ul class="col-10">
            <li>Nom du produit : {{ product.name }}</li>
            <li>Description du produit : {{ product.description }}</li>
            <li>Prix du produit : {{ product.price }}</li>
        </ul>
    </div>
{% endblock %}